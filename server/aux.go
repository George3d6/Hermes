package main

import (
	//"bytes"
	"mime/multipart"
	//"git.cerebralab.com/george/logo"
)

//A package for auxiliary functions

//GetFileSizeInBytes returns the size of a multipart file, in bytes(8-bit)
func GetFileSizeInBytes(fp multipart.File) int64 {
	//var buff bytes.Buffer
	//fileSize, err := buff.ReadFrom(fp)
	//if logo.RuntimeError(err) {
	//    logo.LogDebug("Can't get file size")
	//    return 0
	//}
	//return fileSize;
	return 0
}
